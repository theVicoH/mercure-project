import LoginForm from "@/features/forms/loginForm.";
import Layout from "./layout";

const LoginPage = () => {
  return (
    <Layout>
      <LoginForm/>;
    </Layout>
  );
};

export default LoginPage;
