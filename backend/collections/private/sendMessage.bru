meta {
  name: sendMessage
  type: http
  seq: 2
}

post {
  url: {{url}}/send-message
  body: json
  auth: bearer
}

auth:bearer {
  token: {{toke}}
}

body:json {
  {
    "conversationId": 9,
    "message": "Hello"
  }
}
